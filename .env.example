# Copy this file to .env and fill in real values locally.
# .env is git-ignored and must NEVER be committed.
#
# v1 of this bot is paper-trading only and does not require any broker
# credentials to run. These variables are placeholders for future data-
# provider integrations and for the live-trading gate that does not yet
# have an implementation behind it.

# --- Data provider (market data), if/when a real-time feed is added ---
MARKET_DATA_API_KEY=
MARKET_DATA_API_SECRET=

# --- Historical data provider keys (planning stage only) ---
# These are placeholders for a possible future historical 1-minute
# data import (see docs/real_market_data_plan.md). Nothing in this
# codebase reads or uses these yet, and no automated download exists.
# The current workflow is: acquire CSVs yourself (however you like),
# place them in data/raw/, then run scripts/validate_data.py and
# scripts/run_real_csv_backtest.py — no API key required for that.
# Leave these blank; they are optional and unused by all current tests.
ALPACA_DATA_API_KEY=
ALPACA_DATA_SECRET_KEY=
POLYGON_API_KEY=

# --- IBKR TWS / IB Gateway connection (historical market data only) ---
# These are LOCAL connection settings for a TWS/IB Gateway instance
# running on your own machine -- not account credentials, API keys, or
# secrets. Used only by scripts/download_ibkr_bars.py to fetch closed
# historical bars via reqHistoricalData. No account number, username,
# or password is read from this file or anywhere else in this
# codebase. See docs/ibkr_historical_data_setup.md for setup.
# Default port 7497 is TWS's PAPER TRADING API port.
IBKR_HOST=127.0.0.1
IBKR_PORT=7497
IBKR_CLIENT_ID=101

# --- Live trading gate (inert in v1 — no live broker exists yet) ---
# Must be "true" (and settings.yaml mode.enable_live_trading must also be
# true) before any future live adapter would even be considered. Leave
# this unset or "false".
ENABLE_LIVE_TRADING=false

# --- Future broker credentials (placeholders only, not used in v1) ---
BROKER_API_KEY=
BROKER_API_SECRET=
BROKER_ACCOUNT_ID=
